Add README, makefile and manpage

Add moving/cumulative average of N as awk script
Add card generation script from title/one single line.
Add card generation script that shows the last N lines + blank (or next N lines + blank)
Add sum/avg/moving average awk script
